# Copy to .env.local and set a CARTO Basemaps key:
# https://carto.com/basemaps/apikey/
# This key is sent by the browser and is visible in built assets and tile requests.
# Use a project-specific basemaps key, not a general CARTO workspace API token.
VITE_CARTO_BASEMAPS_KEY=
